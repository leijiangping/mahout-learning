<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.chaohi.recommend.mapper.MoviesMapper">
   <!--表名 -->
	<sql id="tableName">
		movies
	</sql>
	<!-- 实例与数据库映射 -->
	<resultMap id="BaseResultMap" type="movies">
	 			 <id column="id" property="id" javaType="Integer" />
	 		    <result column="name" property="name" javaType="String" />
	 		    <result column="published_year" property="publishedYear" javaType="String" />
	 		    <result column="type" property="type" javaType="String" />
	</resultMap>

	<!-- 字段 -->
	<sql id="Field">
           `id` ,`name` ,`published_year` ,`type` 
	</sql>

	<!-- 字段值 -->
     <sql id="FieldValue">
         #{id} ,#{name} ,#{publishedYear},#{type}
	</sql>
	
	<!-- 更新字段 -->
	<sql id="updateField">
		        `id`=#{id} ,
		        `name`=#{name} ,
		        `published_year`=#{publishedYear} ,
		        `type`=#{type} 
	</sql>
	<!-- 通过ID获取数据 -->
	<select id="findById" parameterType="String" resultMap="BaseResultMap">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		where
		`id` = #{id}
	</select>
	<!-- 通过条件获取数据 -->
	<select id="findByParam" parameterType="map" resultMap="BaseResultMap">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
		<where> 
      	   <if test="id!=null ">
		          and id=#{id}
		    </if>
      	   <if test="name!=null ">
		          and name=#{name}
		    </if>
      	   <if test="publishedYear!=null ">
		          and published_year=#{publishedYear}
		    </if>
      	   <if test="type!=null ">
		          and type=#{type}
		    </if>
		</where> 
	</select>
	
<select id="findByExample" parameterType="movies" resultType="movies"> 
      select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
     <where> 
      	   <if test="id!=null ">
		      and  id=#{id} 
		    </if>
      	   <if test="name!=null ">
		      and  name=#{name} 
		    </if>
      	   <if test="publishedYear!=null ">
		      and  published_year=#{publishedYear} 
		    </if>
      	   <if test="type!=null ">
		      and  type=#{type} 
		    </if>
     </where>   
  </select>
  	
	<!--根据id值集合查询记录集-->
    <select id="findByIds"  parameterType="list"  resultMap="BaseResultMap">
         select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
         where id in 
        <foreach item="item" index="index" collection="list"
            open="(" separator="," close=")"> #{item}
        </foreach>
    </select>
	
	<!-- 查询所有数据 -->
	<select id="findAll" resultMap="BaseResultMap">
		select
		<include refid="Field"></include>
		from
		<include refid="tableName"></include>
	</select>
	
	<!-- 保存 -->
	<insert id="save" parameterType="movies">
		insert into
		<include refid="tableName"></include>
		(
		<include refid="Field"></include>
		) values (
		<include refid="FieldValue"></include>
		)
	</insert>
	
	<!-- 更新 -->
	<update id="update" parameterType="movies">
		update
		<include refid="tableName"></include>
		set
		<include refid="updateField"></include>
		where
		`id` = #{id}
	</update>
	
	<!--批量删除Movies-->
     <select id="deleteMany" parameterType="list">
	        <foreach collection="list" item="item" separator=";">
	            delete from movies where id =  #{item}
	        </foreach>
	 </select>
	  
	 <!--按主键删除Movies-->
	<delete id="deleteOne" parameterType="String">
		delete from movies 	
		<where>
		   `id` = #{id}
		</where>
	</delete>
	</mapper> 